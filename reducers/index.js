import {
  FETCH_QUIZZES,
  FETCH_QUIZ,
  ADD_QUESTION,
  ADD_QUIZ,
} from "../actions"

function quizzes (state=[], action){
  switch(action.type){
    default:
      return state
  }
}

export default quizzes
